import turtle

my_turtle = turtle.Turtle()

my_turtle.shape("turtle")
n = 10
for i in range(0, 22):
    my_turtle.forward(n)
    my_turtle.left(120)
    n += 10

n = 10
for i in range(0, 22):
    my_turtle.forward(n)
    my_turtle.left(120)
    n += 10

n = 10
for i in range(0, 22):
    my_turtle.forward(n)
    my_turtle.left(120)
    n += 10

turtle.done()
